<%= form_for spotify_search_path, method: 'get' do |f|  %>
  <%= f.text_field :name, class: 'search-field', data: {
    autocomplete_source: products_path
  } %>
  <%= f.submit 'Search', :name => nil, class: 'search-btn',  %>
<% end %>
